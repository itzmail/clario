---
phase: 01-tui-refactor-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components.rs
  - src/ui/file_manager.rs
  - src/ui/app_uninstaller.rs
  - src/ui/dashboard.rs
  - src/ui/settings.rs
  - src/app.rs
autonomous: true
requirements: [REFAC-01, REFAC-02]

must_haves:
  truths:
    - "All four draw functions compile with a consistent signature pattern: dashboard and settings use &App, file_manager and app_uninstaller use &mut App"
    - "centered_rect exists only once as a pub fn in src/ui/components.rs — no other copies anywhere"
    - "draw_file_manager no longer takes 12 individual parameters"
    - "cargo build succeeds with zero errors"
  artifacts:
    - path: "src/ui/components.rs"
      provides: "Single public centered_rect implementation"
      contains: "pub fn centered_rect"
    - path: "src/ui/file_manager.rs"
      provides: "Simplified draw function taking &mut App"
      contains: "pub fn draw_file_manager(f: &mut Frame, app: &mut App)"
    - path: "src/ui/dashboard.rs"
      provides: "Draw function taking &App"
      contains: "pub fn draw_dashboard(f: &mut Frame, app: &App)"
    - path: "src/ui/settings.rs"
      provides: "Draw function already taking &App (no change needed)"
      contains: "pub fn draw_settings(f: &mut Frame, app: &App)"
  key_links:
    - from: "src/ui/file_manager.rs"
      to: "src/ui/components.rs"
      via: "import"
      pattern: "use crate::ui::components::centered_rect"
    - from: "src/ui/app_uninstaller.rs"
      to: "src/ui/components.rs"
      via: "import"
      pattern: "use crate::ui::components::centered_rect"
    - from: "src/app.rs"
      to: "src/ui/dashboard.rs"
      via: "draw call"
      pattern: "draw_dashboard\\(f, self\\)"
---

<objective>
Unify all draw function signatures and extract the shared centered_rect helper.

Purpose: Eliminate the 12-parameter draw_file_manager signature and the inconsistent mix of explicit params vs &App/&mut App across screens. Consolidate three duplicate centered_rect implementations into one public function.

Output: All four draw functions use a consistent pattern (dashboard/settings: &App, file_manager/app_uninstaller: &mut App), and centered_rect is a single pub fn in components.rs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tui-refactor-architecture-cleanup/01-RESEARCH.md

<interfaces>
<!-- Current signatures that will change -->

From src/ui/dashboard.rs:
```rust
pub fn draw_dashboard(f: &mut Frame, selected_menu: usize, sys: &System, theme: &AppTheme)
```

From src/ui/file_manager.rs:
```rust
pub fn draw_file_manager(
    f: &mut Frame,
    files: &[FileInfo],
    is_scanning: bool,
    scan_progress_text: &str,
    table_state: &mut ratatui::widgets::TableState,
    show_delete_confirm: bool,
    delete_confirm_selected: u8,
    is_deleting: bool,
    show_archive_confirm: bool,
    archive_confirm_selected: u8,
    is_archiving: bool,
    theme: &AppTheme,
)
```

From src/ui/app_uninstaller.rs:
```rust
pub fn draw_app_uninstaller(f: &mut Frame, app: &mut App)
```

From src/ui/settings.rs:
```rust
pub fn draw_settings(f: &mut Frame, app: &App)  // Already correct — no change needed
```

From src/app.rs callsite (lines 192-239):
```rust
terminal.draw(|f| {
    match self.mode {
        AppMode::Dashboard => {
            draw_dashboard(f, self.selected_menu, &self.sys, &self.config.theme)
        }
        AppMode::FileManager => {
            // scan trigger logic (lines 199-213) — will be moved in plan 01-02
            draw_file_manager(f, &self.scanned_files, self.is_scanning, ...12 params...);
        }
        AppMode::Settings => draw_settings(f, self),
        AppMode::AppUninstaller => draw_app_uninstaller(f, self),
    }
    if self.show_exit_confirm {
        draw_exit_modal(f, self.exit_confirm_selected, &self.config.theme);
    }
})?;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract centered_rect to public in components.rs and remove duplicates</name>
  <files>src/ui/components.rs, src/ui/file_manager.rs, src/ui/app_uninstaller.rs</files>
  <read_first>
    - src/ui/components.rs (current centered_rect at line 11, private)
    - src/ui/file_manager.rs (duplicate centered_rect at line 562)
    - src/ui/app_uninstaller.rs (centered_rect_abs at line 436)
  </read_first>
  <action>
1. In `src/ui/components.rs` line 11: change `fn centered_rect` to `pub fn centered_rect`. No other changes to this function.

2. In `src/ui/file_manager.rs`:
   - Add import at the top of the file (after existing use statements): `use crate::ui::components::centered_rect;`
   - Delete the entire private `fn centered_rect(width: u16, height: u16, r: ratatui::layout::Rect) -> ratatui::layout::Rect` function at lines 562-580 (the duplicate).

3. In `src/ui/app_uninstaller.rs`:
   - Add import at the top of the file (after existing use statements): `use crate::ui::components::centered_rect;`
   - Delete the entire private `fn centered_rect_abs(width: u16, height: u16, r: ratatui::layout::Rect) -> ratatui::layout::Rect` function at line 436+.
   - Replace all calls to `centered_rect_abs(` with `centered_rect(` in the file body. These calls pass the same `(width, height, rect)` args so the substitution is direct.

4. Run `cargo build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/ismailalam/Development/my/clario && cargo build 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn centered_rect" src/ui/components.rs` returns line 11 with `pub fn centered_rect`
    - `grep -c "fn centered_rect" src/ui/file_manager.rs` returns 0 (no local definition)
    - `grep -c "fn centered_rect_abs" src/ui/app_uninstaller.rs` returns 0 (deleted)
    - `grep -c "centered_rect_abs" src/ui/app_uninstaller.rs` returns 0 (no calls remain)
    - `grep "use crate::ui::components::centered_rect" src/ui/file_manager.rs` finds the import
    - `grep "use crate::ui::components::centered_rect" src/ui/app_uninstaller.rs` finds the import
    - `cargo build` succeeds with exit code 0
  </acceptance_criteria>
  <done>centered_rect is a single pub fn in components.rs. No copies exist in file_manager.rs or app_uninstaller.rs. All calls compile.</done>
</task>

<task type="auto">
  <name>Task 2: Unify draw function signatures to &App / &mut App pattern</name>
  <files>src/ui/dashboard.rs, src/ui/file_manager.rs, src/app.rs</files>
  <read_first>
    - src/ui/dashboard.rs (full file — need to replace all param references with app.field access)
    - src/ui/file_manager.rs (full file — need to replace all 12 params with app.field access)
    - src/app.rs (full file — need to update the draw callsites in terminal.draw closure)
    - src/ui/settings.rs (verify it already uses &App — no changes needed)
    - src/ui/app_uninstaller.rs (verify it already uses &mut App — no changes needed)
  </read_first>
  <action>
**A. Migrate draw_dashboard (src/ui/dashboard.rs):**

1. Change signature from:
   `pub fn draw_dashboard(f: &mut Frame, selected_menu: usize, sys: &System, theme: &AppTheme)`
   to:
   `pub fn draw_dashboard(f: &mut Frame, app: &App)`

2. Add import at top: `use crate::app::App;`
   Remove import for `use sysinfo::System;` (no longer a direct param).

3. Throughout the function body, replace:
   - `selected_menu` with `app.selected_menu`
   - `sys` (bare) with `&app.sys` — or `app.sys` depending on context (it was passed as `&System`, now accessed as `&app.sys`)
   - `theme` with `&app.config.theme`
   - `theme.primary()` with `app.config.theme.primary()` (and similarly for all theme method calls)

4. Keep `use crate::models::config::AppTheme;` if it's used for type annotations within the function (e.g., local variables typed as `&AppTheme`). Otherwise remove it.

**B. Migrate draw_file_manager (src/ui/file_manager.rs):**

1. Change signature from the 12-param version to:
   `pub fn draw_file_manager(f: &mut Frame, app: &mut App)`

2. Add import at top: `use crate::app::App;`
   Remove the `FileInfo` import if it's only used for the function param type (keep it if used inside the body — it is used for tree iteration).

3. Throughout the function body, replace all 12 params with app field access:
   - `files` with `&app.scanned_files`
   - `is_scanning` with `app.is_scanning`
   - `scan_progress_text` with `&app.scan_progress_text`
   - `table_state` with `&mut app.file_table_state`
   - `show_delete_confirm` with `app.show_delete_confirm`
   - `delete_confirm_selected` with `app.delete_confirm_selected`
   - `is_deleting` with `app.is_deleting`
   - `show_archive_confirm` with `app.show_archive_confirm`
   - `archive_confirm_selected` with `app.archive_confirm_selected`
   - `is_archiving` with `app.is_archiving`
   - `theme` with `&app.config.theme`

   Note: `table_state` was `&mut TableState` — now it becomes `&mut app.file_table_state`. This is why the signature uses `&mut App`.

**C. Update callsites in src/app.rs (inside terminal.draw closure, lines 192-239):**

1. Change the Dashboard call from:
   `draw_dashboard(f, self.selected_menu, &self.sys, &self.config.theme)`
   to:
   `draw_dashboard(f, self)`

   Note: `self` is `&mut App` inside the closure. `draw_dashboard` takes `&App`. Rust will auto-reborrow `&mut App` to `&App`, so `draw_dashboard(f, self)` compiles.

2. Change the FileManager call. Remove the entire 12-param call block (lines 214-227) and replace with:
   `draw_file_manager(f, self);`

   IMPORTANT: Do NOT move or remove the scan trigger logic at lines 199-213 yet — that is plan 01-02's job. Leave the `if self.scanned_files.is_empty() && !self.is_scanning { ... }` block exactly where it is for now.

3. Settings and AppUninstaller calls remain unchanged (`draw_settings(f, self)` and `draw_app_uninstaller(f, self)` already use the correct pattern).

4. Run `cargo build` to verify.
  </action>
  <verify>
    <automated>cd /Users/ismailalam/Development/my/clario && cargo build 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pub fn draw_dashboard" src/ui/dashboard.rs` shows `pub fn draw_dashboard(f: &mut Frame, app: &App)`
    - `grep "pub fn draw_file_manager" src/ui/file_manager.rs` shows `pub fn draw_file_manager(f: &mut Frame, app: &mut App)`
    - `grep "use crate::app::App" src/ui/dashboard.rs` finds the import
    - `grep "use crate::app::App" src/ui/file_manager.rs` finds the import
    - `grep "draw_dashboard(f, self)" src/app.rs` finds the simplified callsite
    - `grep "draw_file_manager(f, self)" src/app.rs` finds the simplified callsite
    - `grep -c "selected_menu: usize" src/ui/dashboard.rs` returns 0 (old param gone from signature)
    - `cargo build` succeeds with exit code 0
  </acceptance_criteria>
  <done>All four draw functions use a consistent pattern. dashboard and settings take &App, file_manager and app_uninstaller take &mut App. No 12-param signatures remain. cargo build passes.</done>
</task>

</tasks>

<verification>
- `cargo build` compiles with zero errors
- `grep -rn "fn centered_rect" src/ui/` returns exactly ONE result in components.rs
- All four draw functions have consistent signatures (2-param: Frame + App reference)
- No behavioral changes — the app renders identically
</verification>

<success_criteria>
- cargo build succeeds
- centered_rect is a single pub fn in components.rs with no duplicates
- draw_dashboard takes (f, &App), draw_file_manager takes (f, &mut App), draw_settings takes (f, &App), draw_app_uninstaller takes (f, &mut App)
- All callsites in app.rs simplified to draw_X(f, self)
</success_criteria>

<output>
After completion, create `.planning/phases/01-tui-refactor-architecture-cleanup/01-01-SUMMARY.md`
</output>
